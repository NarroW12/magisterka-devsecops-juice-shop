# Python
__pycache__/
*.py[cod]
*$py.class
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.venv/
venv/
env/

# Node
node_modules/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Eksperyment — surowe wyniki SARIF/HTML/JSON (rekonstruowalne z gh artifacts)
data/raw/
data/plots/

# Zagregowane CSV (data/results.csv, data/findings.csv) są wersjonowane —
# stanowią materiał wejściowy do rozdz. 5 i powinny być dostępne bez raw/.

# Materiały referencyjne (poza repozytorium)
_reference_*/

# Sigstore / Cosign artifacts
*.sig
*.pem

# Lokalne overrides
docker-compose.override.yml
.env
.env.local
